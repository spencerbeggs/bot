{
	"name": "@plugin-bot/copilot-plugin",
	"version": "0.3.0",
	"private": true,
	"description": "This package does not publish to npm. It exists to give changesets something to version, so the Copilot plugin manifest is bumped, tagged and released in lockstep."
}
